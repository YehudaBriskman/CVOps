{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Train Config",
  "type": "object",
  "properties": {
    "training_container_id": {
      "type": "string", "format": "uuid",
      "description": "UUID of the training_container whose ICD config drives the Docker run"
    },
    "hyperparams": {
      "type": "object",
      "description": "Key/value pairs forwarded to the container via ICD inputs mapping",
      "additionalProperties": { "type": ["string", "number", "boolean"] }
    }
  },
  "required": ["training_container_id"],
  "additionalProperties": false
}
