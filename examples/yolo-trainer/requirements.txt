# torch / ultralytics / mlflow already live on the worker-training image and are
# visible to the per-run venv (--system-site-packages); pins here are satisfied
# without reinstall. optuna is NOT on the image, so it is installed per run.
ultralytics>=8.0.0
mlflow>=2.0.0
optuna>=3.4.0
pyyaml>=6.0
